
const initialState = {

}

const userReducer = (state = initialState, action: { type: any }) => {
    switch (action.type) {


        default:
            return state;
    }
}

export default userReducer